# 03: Setup skill writes the section on a bare repo

**What to build:** A user runs `/codex-subagent:setup-codex-delegation` in a repo with no `## Codex delegation` section and gets one written into `CLAUDE.md`, else `AGENTS.md`, else the file they choose when asked. The skill first confirms Codex is on PATH and signed in and reports its version, stopping if not. It fetches the OpenAI Codex models documentation, following its redirect, to offer the current model identifiers and effort levels, marks the default model from the user's Codex config, and falls back to that default with any typed name accepted when the fetch fails; no validation runs. It interviews one section per answer, each led by a recommended answer: model per row, effort per row (high, medium, high, high), pause rule (default straight delegation), quiet-run threshold (default three minutes), what Codex cannot run here (default nothing), concurrency cap (default two). It shows the draft, where the user may add rows, each with a label, model, effort, and one of three shapes: read-only run, write run, or review. The section is standalone at the top level and follows the agreed shape: the delegating sentence, the routing table with the four default rows, the research-run guidance, then the behaviour lines. The skill writes nothing outside that file. One delegation driven by the generated section succeeds.

**Blocked by:** 02 (Delegating skill carries mechanics only)

**Status:** ready-for-agent

**Implementation:** delegate the build to an Agent-tool subagent with `model: "opus"`. The main session runs on Fable and only plans, reviews the subagent's diff, and checks the acceptance criteria. Spec: `.scratch/codex-delegation-setup/spec.md`. Governing decision: ADR-0002. Write the new skill with the `writing-for-agents` skill loaded; the repo's own setup skill under `.claude/skills` is a source of ideas for the explore, ask, draft, write flow, not a template to copy.

- [x] A second skill in the plugin resolves as `/codex-subagent:setup-codex-delegation` and has model invocation disabled
- [x] Preflight reports the Codex version and stops with the login command named when Codex is missing or not signed in
- [x] Model discovery fetches the documentation, marks the config default, and falls back to it when the fetch fails
- [x] The interview runs in the agreed order with the agreed defaults, each answer acceptable in a word
- [x] The draft is shown before writing; rows can be added, each naming one of the three shapes; adversarial review is the review shape asked for adversarially, not a fourth shape
- [x] The written section opens with exactly one delegating sentence, carries the routing table, the research-run guidance, and the behaviour lines, and omits the environment line when the answer was nothing
- [x] The section is standalone at the top level of `CLAUDE.md`, else `AGENTS.md`, else the file the user chose when asked; nothing is written elsewhere
- [x] In a disposable repo, one delegation run from a row of the generated section returns a final message

## Comments

- 2026-09-20: implemented on branch `feat/codex-delegation-setup`. The plugin gained a second skill at `plugins/codex-subagent/skills/setup-codex-delegation/SKILL.md` — frontmatter `name: setup` plus `disable-model-invocation: true`, a human-facing description, and no `allowed-tools`, since the skill needs Bash, a fetch, a read and a write. Prose only; no script. Steps are preflight, model discovery, interview, draft, write, done, with a closing `## Lines this skill owns` section naming the delegating sentence, the rows it wrote, the threshold line, the cap line, the stop-after-review line and the environment line as the skill's, and everything else in the section as the user's. Ticket 04's re-run is one sentence of placeholder in the write step (keep it single, take the recommended answers from the current values, rewrite only the owned lines) and nothing more. Written with the `writing-for-agents` skill loaded; the repo's own `setup-matt-pocock-skills` supplied the explore/ask/draft/write shape only. `bash tests/run-tests.sh` reports PASS: 311 FAIL: 0, and `case_plugin_names_no_model` still passes: the skill says "the config default" and describes the docs page rather than naming a model.
- 2026-09-20: docs fetch verified today. `curl -sI https://developers.openai.com/codex/models` answers 308 to `https://learn.chatgpt.com/docs/models`; `curl -sL` on the original URL returns 200 and ~648 KB. The page prints each model identifier as a `codex -m <identifier>` command: recommended `gpt-6-astra`, `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`, and under "View other models" `gpt-5.5` (retires 2026-10-14), `gpt-5.4`, `gpt-5.4-mini`. Its CLI reasoning-level selector lists, lowest first, Low, Medium (default), High, Extra high, Max, Ultra. `~/.codex/config.toml` gives the config default as `gpt-5.6-sol` with `model_reasoning_effort = "high"`, which is what the interview recommends.
- 2026-09-20: dry-ran the skill as the orchestrator in a disposable git repo with no `CLAUDE.md` and no `AGENTS.md`, accepting every recommendation (config default for all four models; high/medium/high/high; straight delegation; 3 minutes; nothing Codex cannot run; cap 2). Preflight printed `codex-cli 0.155.1` and `Logged in using ChatGPT`. With no instructions file present the skill asks which to create, and the dry run chose `CLAUDE.md`; the section landed as a standalone top-level `## Codex delegation` and was the only file created — nothing else in the repo changed. The environment line is absent, as "nothing" requires. Then one delegation from the research row, run from inside that repo: `printf 'Reply with the single word OK.\n' | bash <repo>/plugins/codex-subagent/scripts/codex-subagent.sh run --model gpt-5.6-sol --effort medium --read-only`, exit 0, run directory `/var/folders/f8/rxr5rt517xj0ffgj45w23ncm0000gn/T/codex-subagent/20260920-214452-12276-W9K9eV`, `final-message.md` holds `OK`, and the read-only run left the tree as it was.
- 2026-09-20: two things not observed live. `/codex-subagent:setup-codex-delegation` resolving is by construction — the installed plugin comes from the GitHub marketplace, not this working copy, so the new skill cannot load in a session until 0.3.0 ships; the directory layout and frontmatter match the existing `codex-subagent` skill, which resolves. And the model-discovery fallback (fetch fails, offer the config default, accept any typed name) was not exercised, since the fetch succeeded. Every criterion is ticked. The `Status:` line is left at `ready-for-agent` for the orchestrator to resolve after it reviews the diff.
- 2026-09-20 (fix pass): the draft's research row now carries `--read-only <task>` rather than `[--read-only]`, matching the read-only-run shape in the shapes table and the spec's "research and exploration from any source as a read-only run"; the research guidance below the table still says when to drop the flag. The plugin README's example section was re-derived by running the skill's prose by hand on a fresh disposable repo, every recommendation accepted, and pasted verbatim.
- 2026-09-20: renamed the skill from `setup` to `setup-codex-delegation` at the user's request, to avoid a short-form collision with other plugins' `setup` skills. Directory, frontmatter name, READMEs, spec, and ticket text updated; tests unchanged.
