# 04: Setup skill re-run keeps hand edits

**What to build:** A user re-runs `/codex-subagent:setup-codex-delegation` on a repo whose section already exists and has hand-written lines. The interview starts from the values already in the section, so switching one model is one answer. The write updates only the lines the skill owns: the delegating sentence, the table rows it wrote, the threshold, the cap, the stop rule, and the environment line. Every other line in the section, including rows the user added or reworded and guidance they wrote, stays where it is. The section is never duplicated.

**Blocked by:** 03 (Setup skill writes the section on a bare repo)

**Status:** ready-for-agent

**Implementation:** delegate the build to an Agent-tool subagent with `model: "opus"`. The main session runs on Fable and only plans, reviews the subagent's diff, and checks the acceptance criteria. Spec: `.scratch/codex-delegation-setup/spec.md`. Governing decision: ADR-0002. Edit the skill with the `writing-for-agents` skill loaded.

- [x] On a repo with an existing section, the interview's recommended answers are the section's current values
- [x] After a re-run that changes one model, that row's model and invocation change and nothing else in the section does
- [x] Hand-written lines in the section, and rows the user added or reworded, survive a re-run byte for byte
- [x] A re-run never produces a second `## Codex delegation` section
- [x] The skill text names which lines it owns

## Comments

- 2026-09-20: built in `plugins/codex-subagent/skills/setup-codex-delegation/SKILL.md`, on branch `feat/codex-delegation-setup`. The re-run is a branch inside the existing steps, not a second flow: the file pick moved out of the write step into a new step 3, "The file, and the section already in it", which greps for `^## Codex delegation`, bounds the section at the next top-level `## ` or end of file, and reads the recommendations off it — model and effort from each owned row's cells, the pause rule from which of the two delegating sentences is present, the threshold from the quiet-run line, the cap from the runs-at-once line, the environment list from the "cannot run here" line with absence meaning nothing. A line that is gone or unreadable falls back to step 4's default for that one answer, and the skill says so as it asks. Rows whose Work label the skill did not write (added or reworded) are the user's: the interview passes over them, the write leaves them byte for byte, and the draft names them. Step 4 leads with the current value whenever step 3 found one, so switching one model is one answer. Step 6 writes one edit per owned line, matching the line as it reads now, and calls out that a wholesale rewrite from the draft would flatten the user's lines; the environment line is updated, added at the draft's position, or dropped when the answer is now nothing. The closing `## Lines this skill owns` section is unchanged and still accurate.
- 2026-09-20: dry-ran the re-run prose as the orchestrator in a disposable git repo (`CLAUDE.md` with `## Build` before and `## Release` after; section carrying three pristine default rows, the review row with a reworded label, a hand-added "Changelog drafting" row, a hand-written paragraph about `secrets/`, and a hand-edited 90-second threshold). Preflight printed `codex-cli 0.155.1` and `Logged in using ChatGPT`; the docs fetch listed today's identifiers. (1) Changing only the research row's model: the diff is exactly one line, that row, its Model cell and its Invocation cell — the reworded review row, the added row, the guidance paragraph, the 90-second threshold and the two neighbouring sections are untouched. (2) Changing nothing, with every owned line rewritten from the accepted current values: `cmp` reports the file byte-identical. (3) Answering a non-empty environment list where there was none: the diff is one added line, last of the behaviour lines after the stop-after-review line; running again with nothing removes exactly that line and returns the file byte-identical to the state before. `grep -c '^## Codex delegation'` is 1 after every run. `bash tests/run-tests.sh` reports PASS: 311 FAIL: 0, and `grep -rn 'gpt-' plugins/` is empty.
- 2026-09-20: not observed live — `/codex-subagent:setup-codex-delegation` invoked in a real session, since the installed plugin comes from the marketplace rather than this working copy (same limitation as ticket 03). The dry run followed the prose by hand instead. The `Status:` line is left at `ready-for-agent` for the orchestrator.
